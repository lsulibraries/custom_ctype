# node.type.custom_ctype.yml
langcode: en
status: true
dependencies:
  enforced:
    module:
      - custom_ctype # This is the name of the module we're using for this example
name: 'Custom'
type: custom_ctype
description: 'Content type for testing stuff out.'
new_revision: false
preview_mode: 1
display_submitted: true
